
# CN-Codex 炫酷企业网站 — 重构计划

## 概述

基于 `awesome-design-md` 工作流，彻底重构现有 CN-Codex 企业站。保留现有的 Stripe 风格 DESIGN.md 中的核心品牌色，但在视觉体验上做**质的飞跃**——加入 3D 粒子背景、实时编码终端交互、毛玻璃动效、滚动驱动的视差动画、动态代码块打字效果等"炫酷"元素，打造一个让开发者感到"WOW"的官网。

---

## 步骤 1：更新 DESIGN.md（设计系统升级）

**目标**：在现有设计系统基础上，增加"炫酷"维度——动效系统、3D/粒子风格指南、交互设计规范。

### 要修改的文件
- `DESIGN.md` — 保留现有品牌色/排版/间距/圆角/阴影令牌，新增以下章节：
  - **动效与交互系统**：粒子动画规范、滚动驱动动画时间线、微交互动效（悬停、点击、加载）、打字机效果规范
  - **3D 与视觉特效**：Three.js 粒子系统配色规范、毛玻璃（Glassmorphism）参数、渐变 Mesh 背景配色
  - **交互式组件**：实时编码终端规范、交互式架构图规范
  - **新品牌色扩展**：增加科技蓝绿点缀色（`#00D4AA` 绿松石）、霓虹紫（`#A855F7`）用于代码高亮和特效点缀

---

## 步骤 2：重构 index.html — 全新"炫酷"实现

### 核心视觉增强（6 大亮点）

#### 1. 🎆 动态 3D 粒子背景 (Three.js)
- 在 Hero 区域使用 Three.js 粒子系统（约 2000 个粒子）
- 粒子颜色：品牌紫 `#635BFF` + 白色，缓慢旋转浮动
- 鼠标移动时粒子产生视差反应（Parallax Effect）
- 粒子之间动态连线（类似星空网络效果）
- 性能考量：移动端自动降低粒子数量，`prefers-reduced-motion` 时完全禁用

#### 2. 🖥️ 交互式编码终端 (Hero 右侧)
- 替换现有的 Hero 右侧抽象图形为一个**实时编码终端模拟器**
- 终端窗口：毛玻璃效果边框，带 macOS 风格红绿灯按钮
- 内容：逐行打字输出 CN-Codex 的使用演示（CSS/JS 代码片段）
- 代码语法高亮（使用 Prism.js 或手写颜色）
- 循环播放：输入 → 输出 → 清屏 → 重新输入
- 光标闪烁效果

#### 3. 🌊 滚动驱动视差与动画网格
- 所有 section 入场：不再是简单的淡入上移，而是**分层视差**（背景层/内容层/装饰层各以不同速度移动）
- 装饰性元素：浮动几何体（圆环、六边形、网格线）在滚动时旋转/位移
- 技能网格卡片 hover：3D 翻转 + 发光边缘扫描线动画
- "统计数据"区域：数字跳动时伴有粒子爆发效果

#### 4. 🔮 毛玻璃（Glassmorphism） + 霓虹光效
- 导航栏：更强烈的毛玻璃效果，底部发光边框
- 特性卡片、定价卡片：半透明毛玻璃背景 (`rgba(17, 24, 39, 0.6)` + `backdrop-filter: blur(16px)`)
- 卡片边框：渐变边框（`border-image: linear-gradient(...) 1`）或使用伪类实现
- hover 时霓虹光晕扫描动画

#### 5. 📊 动态架构图可视化
- "强大架构" section：用一个**可交互的 SVG/Canvas 架构图**替代静态卡片列表
- 展示 CN-Codex 的核心架构：MCP Server → Plugin System → Agent/Codey → SmartBrain
- 节点之间流动的光点动画（表示数据流）
- 节点 hover 时弹出详细信息

#### 6. ⌨️ 代码块打字动画
- 在"为什么选择 CN-Codex"或特性区域，展示真实的代码示例
- 代码逐行"键入"显示，伴随光标闪烁
- 语法高亮配色：深色背景 + 霓虹彩色文本

### 页面结构（保持现有 sections，优化内容体验）

| Section | 增强内容 |
|---------|---------|
| **导航** | 增加"交互演示"入口、语言切换开关 |
| **Hero** | Three.js 粒子背景 + 实时编码终端 + CTA 按钮环绕光效 |
| **统计数据** | 粒子爆发数字动画 + 背景微型进度环 |
| **技能矩阵** | 3D 翻转卡片 + 发光扫描线 |
| **架构展示** | 交互式 SVG 流程动画 |
| **特性详解** | 代码打字动画 + 毛玻璃特性卡片 |
| **为什么选择** | 对比表格 + 动态评级条 |
| **定价** | 毛玻璃卡片 + 推荐方案霓虹边框脉冲 |
| **CTA** | 全屏渐变 + 浮动 CTA 按钮动效 |
| **页脚** | 简洁 + 发光分隔线 |

### 技术实现

#### HTML
- 单页面 `index.html`，语义化 HTML5
- 内联所有 CSS（按 DESIGN.md tokens 组织）
- 内联/外链 JavaScript（Three.js 从 CDN 加载，其余内联）

#### CSS 关键升级
```css
/* 新增令牌 */
--accent-teal: #00D4AA;
--accent-neon: #A855F7;
--glass-bg: rgba(17, 24, 39, 0.6);
--glass-border: rgba(255, 255, 255, 0.08);
--glass-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
--neon-glow: 0 0 20px rgba(99, 91, 255, 0.3);
```

#### JavaScript 关键模块
1. **Three.js 粒子系统** — 从 CDN 加载 Three.js，创建粒子场景
2. **Terminal 打字机** — 纯 JS 实现逐行输出 + 循环
3. **滚动动画控制器** — IntersectionObserver + 自定义 easing
4. **架构图动画** — SVG + requestAnimationFrame
5. **代码打字效果** — 自定义 Typing 类
6. **数字计数器增强** — 添加粒子爆发特效

### 响应式策略
- 移动端：Three.js 粒子 → 降级为静态渐变背景；终端模拟器 → 隐藏或简化；3D 翻转卡片 → 普通卡片
- 平板：降低粒子数量（500），保留终端但缩小
- 桌面：全功能体验

### 性能保障
- Three.js 使用 lazy load（`import()` 或 `defer`）
- 使用 `will-change` 提示 GPU 加速
- 所有动画尊重 `prefers-reduced-motion`
- 粒子系统使用 `BufferGeometry` 而非 `Geometry`
- 使用 `IntersectionObserver` 延迟初始化非首屏动画

---

## 步骤 3：验证与测试

- 使用 `python -m http.server` 启动本地静态服务器
- 浏览器打开验证：
  - 粒子背景是否正常渲染
  - 终端打字动画是否流畅
  - 滚动动画是否触达
  - 响应式断点是否正常
  - 移动端降级是否合理
- 使用浏览器 DevTools 检查性能（FPS、内存）

---

## 文件清单

| 操作 | 文件 | 说明 |
|------|------|------|
| **修改** | `DESIGN.md` | 新增动效/3D/交互设计规范令牌 |
| **重建** | `index.html` | 完全重写为炫酷版本（内联 CSS + JS） |

---

## 潜在风险与应对

| 风险 | 应对 |
|------|------|
| Three.js CDN 加载失败 | 降级为 CSS 渐变背景，页面正常显示 |
| 移动端性能问题 | 自动检测设备性能，低端设备禁用 Three.js 和重动画 |
| 浏览器兼容性（backdrop-filter） | 使用 `@supports` 检测，不支持的浏览器使用半透明纯色背景 |
| 代码打字动画与 SSR/SEO 冲突 | 纯客户端 JS 实现，不影响初始内容渲染 |
| `npx serve` 在 Windows 不稳定 | 使用 `python -m http.server 8000` 启动 |

---

## 时间预估

| 阶段 | 预估 |
|------|------|
| 更新 DESIGN.md | ~10 分钟 |
| 编写 index.html（CSS + HTML 结构） | ~30 分钟 |
| 编写 JavaScript（粒子 + 终端 + 动画） | ~30 分钟 |
| 调试与优化 | ~20 分钟 |
| **总计** | **~90 分钟** |


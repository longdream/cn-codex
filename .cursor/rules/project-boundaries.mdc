---
description: CN-Codex 与 Codex 项目边界规范，防止混淆两个项目
alwaysApply: true
---

# 项目边界规范

## 两个项目的角色

- `D:\rustwork\cn-codex` — **唯一工作目标**。所有开发、测试、构建、修改都在此项目中进行。
- `D:\rustwork\codex` — **只读参考库**。仅供阅读代码作为参考，绝对不可修改、测试、运行命令。

## 禁止事项

1. **禁止修改** `D:\rustwork\codex` 下的任何文件
2. **禁止在** `D:\rustwork\codex` 目录下执行 cargo test / cargo build / pnpm 等命令
3. **禁止** cn-codex 的 Cargo.toml 通过 `path = "..."` 依赖 codex 的任何 crate
4. **禁止** cn-codex 的代码中 `use codex_*` 引用来自 codex 仓库的 crate

## 允许事项

1. **允许读取** `D:\rustwork\codex` 的源代码作为参考
2. **允许参考** codex 的实现逻辑，在 cn-codex 中**重新实现**
3. cn-codex 是完全独立的项目，有自己的 protocol 类型和 JSON-RPC 客户端

## 架构

cn-codex 通过启动外部 `codex` 进程并使用 stdio JSON-RPC 通信，而非内嵌 codex-core。
所有协议类型在 `src-tauri/src/protocol/` 中本地定义。

# Task: qa-27-markdown-rendering

你是在 CN-Codex 仓库中工作的 coding agent。

## 目标
阅读 Markdown 渲染相关代码，写入 answer.json。

## 问题
1. 使用了哪个 Markdown 渲染库？
2. 是否支持代码高亮？
3. 是否支持 LaTeX 公式？
4. Markdown 渲染组件在哪个文件？

## 输出格式（必须严格）
在工作目录创建 answer.json 包含上述问题的答案。
完成后即可结束。
# Task: qa-11-types-definitions

你是在 CN-Codex 仓库中工作的 coding agent。

## 目标
阅读 src/types 目录，回答类型定义问题，写入 answer.json。

## 问题
1. types 目录下有几个文件？
2. 主要定义了哪些核心类型？
3. 是否存在 Message 类型定义？
4. 是否存在 Thread 类型定义？

## 输出格式（必须严格）
在工作目录创建 answer.json 包含上述问题的答案。
完成后即可结束。
# Task: qa-02-config-provider

你是在 CN-Codex 仓库中工作的 coding agent。

## 目标
阅读 codey/config.toml，回答配置问题，写入 answer.json。

## 问题
1. 当前默认 model 字段值是什么？
2. 当前默认 model_provider 是什么？
3. web_search 配置值是什么？
4. approval_policy 配置值是什么？

## 输出格式（必须严格）
在工作目录创建 answer.json 包含上述问题的答案。
完成后即可结束。
# Task: qa-30-workspace-config

你是在 CN-Codex 仓库中工作的 coding agent。

## 目标
阅读项目配置文件，写入 answer.json。

## 问题
1. 是否存在 .vscode 配置目录？
2. 编辑器推荐扩展有哪些？
3. 是否存在 .editorconfig？
4. TypeScript strict 模式是否启用？

## 输出格式（必须严格）
在工作目录创建 answer.json 包含上述问题的答案。
完成后即可结束。
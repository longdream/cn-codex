# Task: qa-10-api-layer

你是在 CN-Codex 仓库中工作的 coding agent。

## 目标
阅读 src/api 目录，回答 API 层问题，写入 answer.json。

## 问题
1. api 目录下有哪些模块文件？
2. 是否使用了 fetch 或 axios？
3. 是否存在请求拦截器？
4. API base URL 如何配置？

## 输出格式（必须严格）
在工作目录创建 answer.json 包含上述问题的答案。
完成后即可结束。